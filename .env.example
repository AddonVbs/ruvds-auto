# Telegram
TELEGRAM_TOKEN=123456:AA...
OWNER_TG_ID=123456789

# RuVDS API v2 token from https://ruvds.com/my/settings/api
RUVDS_TOKEN=

# Defaults for server creation (see ruvds-api-v2.yaml)
# datacenter / tariff / os IDs — get from GET /v2/datacenters, /v2/tariffs, /v2/os
DEFAULT_DATACENTER=1
DEFAULT_TARIFF_ID=14
DEFAULT_OS_ID=52
DEFAULT_PAYMENT_PERIOD=2
DEFAULT_CPU=2
DEFAULT_RAM=2
DEFAULT_DRIVE=20
DEFAULT_DRIVE_TARIFF_ID=3
DEFAULT_IP_COUNT=6
COMPUTER_NAME=auto-vds

# Postgres (currently unused, kept for future)
Password=
